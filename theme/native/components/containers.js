import { Borders } from '../styles';

export const grayContainer = {
  container: {
    backgroundColor: '#f5f5f5',
  },
};

export const borderContainer = {
  container: {
    ...Borders.defaultBorder,
  },
};

export const borderBottomContainer = {
  container: {
    ...Borders.bottomBorder,
  },
};

export const messageInputContainer = {
  container: {
    maxHeight: 100,
  },
};
